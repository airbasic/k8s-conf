apiVersion: v1
kind: Service
metadata:
  labels:
    name: registry2-azure
  name: registry2-azure
spec:
  ports:
  - name: "web"
    port: 5000
    protocol: TCP
    targetPort: 5000
    nodePort: 30050
  type: NodePort
  selector:
    name: registry2-azure
